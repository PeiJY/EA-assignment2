        close all;
        clear;
        date = load("logs\problem001run001_generation_log.txt");
        
        x=date(:,3);
        y=date(:,2);
        z=date(:,1);

        scatter3(x,y,z,'.')
        xlabel('indiv')
        ylabel('fitness')
        zlabel('fes')

